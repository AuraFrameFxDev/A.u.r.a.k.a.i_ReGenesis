# JDK & JVM
# Explicitly use Java 25.0.2 for Gradle builds (Windows path)
# Enable Gradle's automatic Java toolchain detection as fallback
org.gradle.java.installations.auto-detect=true
org.gradle.java.installations.auto-download=true
# Optimized for 16GB RAM system (Ryzen 7 5700X)
# Enable headless mode to prevent IntelliJ IDEA APIs from initializing in CI
# Idea force core classloader fix for KSP (KT-73255)
org.gradle.jvmargs=-Xmx10g -Dfile.encoding=UTF-8 -Djava.awt.headless=true

# AGP 9.0 / Hilt Compatibility Bridge
# CRITICAL: KSP is not compatible with AGP's built-in Kotlin - must disable
# Performance & Modern Defaults
android.useAndroidX=true
android.nonTransitiveRClass=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.workers.max=2

# KSP / Kotlin 2.3 Alignment
ksp.incremental=true
<<<<<<< HEAD
4ksp.kotlinApiVersion=2.3
ksp.kotlinLanguageVersion=2.3
=======
ksp.kotlinApiVersion=2.4
ksp.kotlinLanguageVersion=2.4
>>>>>>> regenesis_dev/Launch
# Disable KSP2 for YukiHook API compatibility

# ─────────────────────────────────────────────────────────────────────────────
# Kotlin daemon / incremental build stability (Windows)
# Avoid rare FileNotFound/NoSuchFile issues during incremental .class moves.
# ─────────────────────────────────────────────────────────────────────────────
kotlin.daemon.enabled=false
kotlin.compiler.execution.strategy=in-process
kotlin.incremental=false

# NDK
android.ndkVersion=29.0.14206865
org.gradle.configuration-cache=true
# ═══════════════════════════════════════════════════════════════════════════
# 🔑 API KEYS & CLOUD CONFIGURATION
# ═══════════════════════════════════════════════════════════════════════════
# ? API KEYS & CLOUD CONFIGURATION (MOVED TO local.properties)
# Google Vertex AI / Gemini
# GEMINI_API_KEY=
# NVIDIA Nemotron
# NVIDIA_API_KEY=
# Anthropic Claude
# ANTHROPIC_API_KEY=
# xAI Grok
# XAI_API_KEY=
# Firebase Project Configuration
FIREBASE_PROJECT_NUMBER=35417750637
FIREBASE_PROJECT_ID=auraframefx
FIREBASE_STORAGE_BUCKET=auraframefx.firebasestorage.app
FIREBASE_OAUTH_CLIENT_ID=35417750637-4m0mong9mjselgr4milhc4mamu5706nu.apps.googleusercontent.com
