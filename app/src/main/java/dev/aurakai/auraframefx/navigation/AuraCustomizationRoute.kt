// DELETED - AuraCustomizationRoute.kt was an empty enum with no entries.
// Route definitions live in NavDestination.kt (single source of truth).
package dev.aurakai.auraframefx.navigation

import androidx.compose.ui.graphics.Color

enum class AuraCustomizationRoute {
    ;

    class ColorBlendr(
        id: String,
        title: String,
        subtitle: String,
        styleADrawable: String,
        styleBDrawable: String,
        fallbackDrawable: String,
        route: Any,
        accentColor: Color
    ) {

    }

}
