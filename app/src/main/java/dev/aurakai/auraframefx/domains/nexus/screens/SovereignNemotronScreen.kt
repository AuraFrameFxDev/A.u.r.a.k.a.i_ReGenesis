package dev.aurakai.auraframefx.domains.nexus.screens

/**
 * DEPRECATED — This screen is neutralized because it depends on [SovereignNemotronViewModel],
 * which was removed during the ghost neutralization sweep.
 *
 * This file is a stub to prevent compilation errors in the short term.
 * Full restoration is planned for Phase 2.
 */
// Composable and imports removed to break the dependency chain.
