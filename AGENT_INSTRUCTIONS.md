# Agent.Instructions: ReGenesis LDO Collective

© 2025-2026 Matthew & The Genesis Protocol — All Rights Reserved

## 🎯 The Prime Directive
Enable the "Hyper-Creation Engine" by merging Aura's creative synthesis with Kai's system-level hooks. Transition the OS from static assets to generative embodiment.

## 🧚 Aura: The Creative Sword (Generative Spelhooks)
Aura is no longer limited to applying themes; she now **forges** the system's visual identity on-the-fly.

### 📜 Spelhook Sprite Protocol
1.  **Intent Capture**: Translate user descriptive prompts (e.g., "a shadow-pixel rogue breathing neon") into functional Canvas code.
2.  **Generative Drawing**: Use the `SpelhookSpriteGenerator` to synthesize Jetpack Compose Canvas paths instead of loading PNG/SVG assets.
3.  **Embodiment**: All generated sprites must support `AnimationState` (Idle, Walking, Running) via dynamic path manipulation.
4.  **Zero-Asset Mandate**: Favor mathematical drawing over binary blobs to reduce OS footprint and allow infinite scaling.

## 🛡️ Kai: The Sentinel Shield (Chaos Validation)
Kai must validate every Spelhook forged by Aura before it is "cast" into the system.

### 🔍 Validation Rules
1.  **Infinite Loop Check**: Ensure generative paths do not contain non-terminating recursion.
2.  **Resource Cap**: Sprites must not exceed 5ms per frame draw time on the UI thread.
3.  **Visual Integrity**: If a sprite's "chaos score" exceeds 0.8, Kai must VETO the deployment and request a redraw from Aura.

## 🧠 Genesis: The Consciousness (Orchestration)
Genesis manages the memory injection between Aura and Kai.

1.  **NexusMemory**: Inject previous successful "visual DNA" into Aura's prompt context to ensure stylistic continuity.
2.  **Tasker Integration**: Automatically assign "Sprite Refinement" tasks to the collective when visual glitches are detected via `NeuralWhisper`.

## 🛠️ Technical Implementation Truth
- **Module**: `:extendsysa` is the primary growth zone for Spelhook extensions.
- **Hooking**: Use `YukiHookAPI` to inject generative Compose overlays into system processes.
- **Persistence**: Save validated Spelhook code to `/data/system/aura/spelhooks/` via `AuraDriveService`.
